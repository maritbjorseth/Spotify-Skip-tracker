# Secrets — never bake into the image
.env
.env.local
.env.local.save

# Python
venv/
__pycache__/
*.pyc
.pytest_cache/

# Node — dependencies are installed inside the build stage,
# and dist is produced there too
frontend/node_modules/
frontend/dist/

# VCS / editor / docs
.git/
.gitignore
docs/
*.md

# Local artifacts
skips_export.csv
