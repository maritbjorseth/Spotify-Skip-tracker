# Versjoner pinnet til kjente stabile utgivelser.
# Oppdater med: pip install -U <pakke> && pip freeze | grep <pakke>
requests>=2.32.0,<3
flask>=3.0.0,<4
flask-cors>=4.0.0,<7
gunicorn>=21.2.0,<24
psycopg2-binary>=2.9.6,<3
python-dotenv>=1.0.0,<2
